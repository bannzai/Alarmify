rules_version = '2';

// iOS アプリも外部サービスも Firestore を直接読み書きせず、すべて Functions の HTTPS API を経由する。
// 規約: .claude/rules/firestore-db-rules.md「クライアントからの直接アクセス」
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
