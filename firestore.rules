rules_version = '2';

// iOS アプリも外部サービスも Firestore を直接読み書きせず、すべて Functions の API を経由する
// (.claude/rules/firestore-db-rules.md)。そのためクライアントからのアクセスは全パスで拒否する
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
