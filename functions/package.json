{
  "name": "alarmify-functions",
  "version": "0.1.0",
  "private": true,
  "description": "Alarmify のバックエンド (Cloud Functions gen2)",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && firebase emulators:exec --project demo-alarmify --only auth,firestore,functions \"vitest run\"",
    "serve": "npm run build && firebase emulators:start --project demo-alarmify --only auth,firestore,functions"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "firebase-tools": "^15.28.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
